git init
git config --global user.name "name"
git config --global user.email "email"
git -l 显示当前列表
git push -u origin master 登陆
git clone 克隆远程仓库
git push 添加到远程仓库
git add -A 上传所有文件到缓冲区




git add README.md
git commit -m "first commit"
git remote add origin https://github.com/arch123A/RegExp.git

git remote add origin https://github.com/arch123A/RegExp.git
git push -u origin master
