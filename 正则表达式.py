# ？加下列字母['a', 'i', 'L', 'm', 's', 'u', 'x']放在开头，第一个字符对应 一种匹配标志，
# re-M多行式，re-I忽略大小写，，re-A只匹配ASCII字符。
#re.search ，在字符串中搜索正则表达式第一次出现的位置.'.span'可以找到匹配的位置，是从零开始算起的。
#re.match 尝试从字符串的起始位置匹配一个模式，如果不是起始位置匹配成功的话，match()就返回none。